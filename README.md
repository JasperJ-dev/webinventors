# React App

Static React website, built with Vite. It does not require a WordPress backend.

## Run locally

Install Node.js 20.19 or newer, then run:

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The production output will be generated in `dist/` and can be auto-deployed to destination host.
